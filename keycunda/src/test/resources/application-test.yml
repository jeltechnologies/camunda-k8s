spring:
  datasource:
    url: jdbc:h2:mem:keycunda;MODE=PostgreSQL
    username: sa
    password: ""

keycunda:
  camunda-domain: keycunda.example.com
  demo-user:
    name: Demo User
    email: demo@example.com
    password: demo
  clients:
    identity:
      secret: test-secret
      audience: camunda-identity-resource-server
    orchestration:
      secret: test-secret
      audience: orchestration-api
    connectors:
      secret: test-secret
      audience: orchestration-api
    optimize:
      secret: test-secret
      audience: optimize-api
    web-modeler:
      client-api-audience: web-modeler-api
      public-api-audience: web-modeler-public-api
